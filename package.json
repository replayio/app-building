{
  "name": "app-building",
  "version": "1.0.0",
  "description": "Simple agent loop for building apps with Claude Code CLI",
  "scripts": {
    "loop": "tsx src/loop.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "commander": "^14.0.3"
  }
}
