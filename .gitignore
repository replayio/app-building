node_modules/
dist/
.env
prompt-*.txt
# worker-current.log is the live log file written to during a worker run.
# It is archived to worker-<timestamp>.log before each commit (see log.ts).
# Only the archived timestamped files should be committed.
*/worker-current.log
*~

# Container agent state and home directory
.agent-state.json
.container-registry.jsonl
.agent-home
.claude

# Local Netlify folder
.netlify

# Artifacts
logs/test-run-*
**/playwright-report/*
